name: hepsie_hekabe_app
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hepsie_hekabe_domain:
    path: ../hepsie_hekabe_domain
  hepsie_hekabe_infrastructure:
    path: ../hepsie_hekabe_infrastructure

  mockito: ^4.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/rimbaud.txt
